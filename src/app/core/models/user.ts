export interface User {
      id?: number;
      name: string;
      email: string;
      phoneNumber: string;
      verified_at?: string;
      role: string;
      createdAt: string;
      modifiedAt: string;
}
